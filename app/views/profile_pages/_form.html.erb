<%= form_with(model: profile_page, local: true) do |form| %>
  <% if profile_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_page.errors.count, "error") %> prohibited this profile_page from being saved:</h2>

      <ul>
        <% profile_page.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
